<form method="post" action="{{ path('app_telephones1_delete', {'id': telephones1.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ telephones1.id) }}">
    <button class="btn">Delete</button>
</form>
