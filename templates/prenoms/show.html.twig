{% extends 'base.html.twig' %}

{% block title %}Prenoms{% endblock %}

{% block body %}
    <h1>Prenoms</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ prenom.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ prenom.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_prenoms_index') }}">back to list</a>

    <a href="{{ path('app_prenoms_edit', {'slug': prenom.slug}) }}">edit</a>

    {{ include('prenoms/_delete_form.html.twig') }}
{% endblock %}
