<form method="post" action="{{ path('app_retards_personnel_delete', {'id': retards_personnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retards_personnel.id) }}">
    <button class="btn">Delete</button>
</form>
