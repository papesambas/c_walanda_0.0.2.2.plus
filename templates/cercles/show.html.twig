{% extends 'base.html.twig' %}

{% block title %}Cercles{% endblock %}

{% block body %}
    <h1>Cercles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cercle.id }}</td>
            </tr>
            <tr>
                <th>Designation</th>
                <td>{{ cercle.designation }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cercles_index') }}">back to list</a>

    <a href="{{ path('app_cercles_edit', {'slug': cercle.slug}) }}">edit</a>

    {{ include('cercles/_delete_form.html.twig') }}
{% endblock %}
