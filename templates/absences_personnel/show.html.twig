{% extends 'base.html.twig' %}

{% block title %}AbsencesPersonnel{% endblock %}

{% block body %}
    <h1>AbsencesPersonnel</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ absences_personnel.id }}</td>
            </tr>
            <tr>
                <th>Jour</th>
                <td>{{ absences_personnel.jour ? absences_personnel.jour|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>IsJustify</th>
                <td>{{ absences_personnel.isJustify ? 'Yes' : 'No' }}</td>
            </tr>
            <tr>
                <th>Motif</th>
                <td>{{ absences_personnel.motif }}</td>
            </tr>
            <tr>
                <th>Heure</th>
                <td>{{ absences_personnel.heure ? absences_personnel.heure|date('H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ absences_personnel.createdAt ? absences_personnel.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdatedAt</th>
                <td>{{ absences_personnel.updatedAt ? absences_personnel.updatedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ absences_personnel.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_absences_personnel_index') }}">back to list</a>

    <a href="{{ path('app_absences_personnel_edit', {'id': absences_personnel.id}) }}">edit</a>

    {{ include('absences_personnel/_delete_form.html.twig') }}
{% endblock %}
