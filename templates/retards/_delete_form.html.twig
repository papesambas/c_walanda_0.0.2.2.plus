<form method="post" action="{{ path('app_retards_delete', {'id': retard.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ retard.id) }}">
    <button class="btn">Delete</button>
</form>
