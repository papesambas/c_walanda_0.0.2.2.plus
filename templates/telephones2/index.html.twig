{% extends 'base.html.twig' %}

{% block title %}Telephones2 index{% endblock %}

{% block body %}
    <h1>Telephones2 index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Numero</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for telephones2 in telephones2s %}
            <tr>
                <td>{{ telephones2.id }}</td>
                <td>{{ telephones2.numero }}</td>
                <td>
                    <a href="{{ path('app_telephones2_show', {'slug': telephones2.slug}) }}">show</a>
                    <a href="{{ path('app_telephones2_edit', {'slug': telephones2.slug}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_telephones2_new') }}">Create new</a>
{% endblock %}
